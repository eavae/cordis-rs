[package]
name = "cordis-fixture-hello"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
cordis-sdk = { path = "../../crates/cordis-sdk", default-features = false }

[lints.rust]
unsafe_code = "allow"
