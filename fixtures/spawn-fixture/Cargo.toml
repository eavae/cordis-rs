[package]
name = "cordis-fixture-spawn"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]
test = false

[dependencies]
cordis-sdk = { path = "../../crates/cordis-sdk", default-features = false }

[lints.rust]
unsafe_code = "allow"
