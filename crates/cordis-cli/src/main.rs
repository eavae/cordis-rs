fn main() -> anyhow::Result<()> {
    // Scaffold placeholder: the full startup path (config -> root ->
    // loader -> `.so` plugin tree -> signal handling) lands in H1.
    eprintln!("cordis-cli: not implemented yet (story card H1)");
    Ok(())
}
